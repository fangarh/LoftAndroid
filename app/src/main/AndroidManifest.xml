<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dds.loftmoney">

    <application
        android:name="com.dds.core.LoftMoney"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.logon.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.logon.LogonActivity">

        </activity>
        <activity
            android:name=".activity.addbudget.AddItemActivity"
            android:theme="@style/AddItemTheme" />
        <activity android:name=".activity.displaybudget.MainActivity"></activity>
    </application>

</manifest>